import 'package:flutter/material.dart';

class TravelBuddyShootPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Travel buddy '),
      ),
      body: Center(
        child: Text("this is my page"),
      ),
    );
  }
}
